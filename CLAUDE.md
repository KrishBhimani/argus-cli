# CLAUDE.md

This repository uses the **DOX framework** — an `AGENTS.md` hierarchy of binding
work contracts. `AGENTS.md` (repo root) is the source of truth, including the full
development workflow (branch → PR → squash-merge → release → tag → publish).

**Before editing any path:** read the root `AGENTS.md`, then every `AGENTS.md` on
the route down to that path; the nearest doc controls local detail. **After a
meaningful change:** run the DOX closeout (update the nearest owning doc and any
affected parent/child indexes). Re-read the chain in-session — don't rely on memory.

The root contract (imported in full below):

@AGENTS.md
